version: '2'
services:
  maxapi:
    build: .
    command: bash -c 'rails s -b 0.0.0.0 -p 9292'
    ports:
      - "9292:9292"
    volumes:
      - $PWD:/opt/maxapi
    volumes_from:
      - container:mrmcompose_maxscale_1:ro
